# Meta App Review dossier — submission 1836338617331298

Last verified: 2026-08-24

This dossier is the canonical non-secret preparation record for Meta App Review submission `1836338617331298` for App `NUVANX Reporting` (`1836302544001572`) under Business Portfolio `897835716596010`.

Do not put access tokens, App Secrets, or reviewer passwords in this file.

## Submission decision

**Do not submit yet.** The permission set should be reduced to the minimum reproducible scope and three pre-submit gates remain open: Business Verification, the Dashboard data-deletion URL, and a reviewer-reproducible `ads_management` action.

### Scope to keep in the first review

1. Marketing API Access Tier
2. `ads_management`
3. `ads_read`
4. `business_management`
5. `pages_show_list`
6. `pages_read_engagement`
7. `pages_manage_ads`
8. `leads_retrieval`

### Scope to remove/defer from the first review

- Threads oEmbed Read
- Meta oEmbed Read
- Live Video API
- Business Asset User Profile Access
- `threads_business_basic`
- `email`
- `manage_fundraisers`
- `pages_manage_metadata`
- `publish_video`
- `ads_mcp_management`
- `pages_messaging`
- `catalog_management`
- `threads_basic`
- `instagram_basic`
- `whatsapp_business_messaging`
- `public_profile`
- `whatsapp_business_management`

`pages_manage_metadata` is intentionally deferred. The backend contains a real Lead Ads webhook handler, but automated Page webhook subscription management is not part of this first review. Lead retrieval itself is real and already validated through direct Lead Ads reads.

## Canonical business assets

| Asset | Canonical value |
| --- | --- |
| Business Portfolio | `897835716596010` |
| Meta App | `NUVANX Reporting` |
| App ID | `1836302544001572` |
| System User | `122098243371455164` (`Conversions API System User`) |
| Ad Account | `act_718120894191565` |
| Facebook Page | `1329458703573874` |
| Lead Form | `1493697602775666` |
| RSV26 Campaign | `120249780276630419` |
| Instagram business user | `17841474094610850` (`nuvanx_`) |
| App Domain | `nuvanx.com` |

## Reviewer web access

Stable production URL:

`https://frontend-arisofias-projects-c2217452.vercel.app/`

The frontend authenticates with Supabase email/password and redirects successful sign-in to `/dashboard`.

Reviewer credentials must be provided in Meta only after a dedicated test account is created and tested. Do not use placeholders in the submitted form and do not commit the password here.

Reviewer email: **PENDING**

Reviewer password: **PENDING — secret, Meta submission only**

Support contact shown by the login UI: `support@nuvanx.com`.

## `instructions-web-2` — reviewer instructions

Use the following text after a dedicated reviewer account has been created and verified:

> La aplicación es una plataforma web interna utilizada por NUVANX Medicina Estética Láser para consultar y gestionar información relacionada con sus propias campañas publicitarias de Meta y con los leads generados por esas campañas.
>
> 1. Abra `https://frontend-arisofias-projects-c2217452.vercel.app/`.
> 2. Introduzca el correo y la contraseña de la cuenta de revisión proporcionados en esta solicitud.
> 3. Pulse **Entrar**. Tras autenticarse, la aplicación redirige a **Centro de control** (`/dashboard`).
> 4. Abra **Marketing** y seleccione **Meta Ads**.
> 5. En **Campañas** puede consultar las campañas de la cuenta Meta NUVANX, su estado, objetivo, presupuesto y métricas de rendimiento como gasto, impresiones, alcance, clics, CTR, CPC, CPM y conversiones.
> 6. Abra **Por anuncio** para consultar los anuncios y sus métricas.
> 7. Abra **CRM** para revisar los leads procedentes de Meta Lead Ads. El backend recibe el `leadgen_id` y recupera desde Meta los datos autorizados del lead y su atribución publicitaria.
> 8. Para la prueba de `ads_management`, siga la acción de gestión indicada específicamente en la sección de esa permission una vez que el control de prueba esté disponible en producción. Esa acción debe trabajar únicamente con activos NUVANX y no debe activar gasto publicitario de forma inesperada.
> 9. Todas las pruebas deben realizarse únicamente con activos comerciales propiedad de NUVANX. No se requieren activos personales del revisor.
>
> Activos de referencia: Business Portfolio `897835716596010`; App `1836302544001572`; Ad Account `act_718120894191565`; Facebook Page `1329458703573874`; Lead Form `1493697602775666`; Campaign `120249780276630419`.

The final Meta submission must replace step 8 with the exact tested UI action. Do not claim a create/edit/pause control until that control exists in the deployed product.

## Permission narratives

### Marketing API Access Tier

**Use case**

NUVANX Reporting is an internal business application used to read and manage NUVANX-owned advertising assets through the Meta Marketing API. The application is not a consumer social app and is not being submitted to manage unrelated third-party businesses in this first review.

**Technical evidence**

The canonical runtime token belongs to App `1836302544001572` and System User `122098243371455164`. Existing production audits have successfully read the account, campaign/ad-set/ad/creative surfaces and campaign insights. The management tooling has also performed real write operations against existing NUVANX ad objects, including successful creation of a PAUSED ad with an existing `creative_id`.

**Reviewer evidence needed**

Show the Marketing section reading live campaign/ad data. For any requested management permission, also show the reviewer-safe management action described below.

### `ads_read`

**How the app uses it**

The app reads NUVANX advertising account data and reporting metrics so authenticated NUVANX users can monitor campaign and ad performance inside the internal Marketing dashboard.

**User-facing path**

Login → Marketing → Meta Ads → Campañas / Por anuncio.

**Data shown**

Campaign/ad identifiers and names, status, objective, budget, spend, impressions, reach, clicks, CTR, CPC, CPM, conversions and related reporting fields.

**Representative Graph use**

- Read ad-account campaign collections.
- Read ads and creatives associated with the NUVANX account.
- Read Marketing API insights for the selected date range.

**Expected reviewer result**

The reviewer sees live NUVANX campaign and ad data in the Marketing interface.

### `ads_management`

**How the app uses it**

The backend contains real server-side Marketing API management tooling for NUVANX-owned campaign, ad-set, ad and creative objects. The canonical management token has been verified to include `ads_management`, and production testing has demonstrated successful writes to existing ad objects and successful creation of a PAUSED ad when reusing an existing `creative_id`.

**Important review constraint**

The current production Marketing frontend exposes reporting/read controls but does **not** yet expose a reviewer-reproducible create/edit/pause management control. Do not submit this permission with a fictitious UI path.

**Required pre-submit implementation**

Expose one bounded reviewer-safe management action in the authenticated web app. It must:

- operate only on a designated NUVANX review/test ad object;
- default to `PAUSED` and never unexpectedly start spend;
- make a real Marketing API write using the canonical server-side credential;
- display the resulting Meta object ID/status or updated state;
- provide deterministic rollback/cleanup;
- log the action for audit evidence;
- prevent arbitrary asset IDs or arbitrary account selection.

**Recommended reviewer test**

Use an existing designated test ad and provide a single **Pause / restore PAUSED state** or equivalent idempotent management action. Avoid requiring creation of a new creative while the app remains in Development mode, because Meta currently rejects new creative creation for this app with `code=100`, `error_subcode=1885183`.

**Submission status**

**BLOCKED until the web management action is deployed and tested end-to-end.**

### `business_management`

**How the app uses it**

The application uses Business Manager context to resolve and validate the NUVANX business assets that the canonical system user is allowed to access. It is used to verify the relationship between the Business Portfolio and owned advertising assets used by the internal reporting/management workflow.

**Canonical business**

Business Portfolio `897835716596010`.

**Expected reviewer result**

The app operates only against the NUVANX business/ad assets assigned to the canonical system user; the review should not require the reviewer to supply a separate personal business portfolio.

**Evidence**

Production App Review core-call run `32773483796` recorded `APP_REVIEW_business_management=PASS`.

### `pages_show_list`

**How the app uses it**

The Page permission is used as part of the NUVANX Lead Ads flow to identify/validate the NUVANX Facebook Page associated with Lead Ads and to support Page-scoped Lead Ads access.

**Canonical Page**

`1329458703573874`.

**Expected reviewer result**

The application can resolve the NUVANX Page that owns the Lead Ads form used by the business.

**Evidence**

Production App Review core-call run `32773483796` recorded `APP_REVIEW_pages_show_list=PASS`.

### `pages_read_engagement`

**How the app uses it**

The permission is used for the Page context required by the NUVANX Lead Ads retrieval workflow and Page-associated business data used by the application.

**Canonical Page**

`1329458703573874`.

**Expected reviewer result**

The backend can read the authorized Page context used by the NUVANX Lead Ads flow.

**Evidence**

Production App Review core-call run `32773483796` recorded `APP_REVIEW_pages_read_engagement=PASS`.

### `pages_manage_ads`

**How the app uses it**

The permission is requested for the NUVANX Page/ad relationship required by the business's Lead Ads and advertising workflow. The app uses only the NUVANX Page and its advertising assets.

**Canonical Page**

`1329458703573874`.

**Expected reviewer result**

The reviewer can verify that the Page used by the Lead Ads form belongs to the same NUVANX business advertising flow.

**Evidence**

Production App Review core-call run `32773483796` recorded `APP_REVIEW_pages_manage_ads=PASS`.

### `leads_retrieval`

**How the app uses it**

NUVANX uses Meta Lead Ads to collect requests for medical-aesthetic valuation. The backend receives a Meta `leadgen_id`, retrieves the authorized lead record from Graph API, persists the lead and its advertising attribution, and exposes the resulting lead in the authenticated CRM workflow.

**Canonical Lead Form**

`1493697602775666`.

**Representative lead fields retrieved by the backend**

`field_data`, `created_time`, `ad_id`, `ad_name`, `form_id`, `form_name`, `campaign_id`, `campaign_name`, `adset_id`, `adset_name`, `page_id`, `is_organic`, and `platform`.

**Reviewer path**

Login → CRM. Use a lead associated with the canonical form or the review test lead supplied in the submission evidence.

**Expected reviewer result**

The lead is visible in CRM with its Meta attribution context after retrieval from Meta.

**Evidence**

Production App Review core-call run `32773483796` recorded `APP_REVIEW_leads_retrieval=PASS`. Earlier production auditing also verified a real `/{form}/leads` read for the active NUVANX form.

## Production evidence already available

App Review core-call run `32773483796`:

- `APP_REVIEW_ads_read=PASS`
- `APP_REVIEW_business_management=PASS`
- `APP_REVIEW_pages_show_list=PASS`
- `APP_REVIEW_pages_read_engagement=PASS`
- `APP_REVIEW_pages_manage_ads=PASS`
- `APP_REVIEW_leads_retrieval=PASS`
- `APP_REVIEW_marketing_api_read=PASS`
- `APP_REVIEW_CORE_READ_CALLS=PASS`

Other canonical evidence:

- `32761212623` — full Meta use-case read-only audit.
- `32761521018` — Page-token / Messenger / Instagram / WhatsApp detail audit.
- `32762760660` — app-domain/publication metadata audit.
- `32763162619` — Business System User inventory/token identity reconciliation.
- `32768146699` / job `97562196706` — Graph v22 app-object field probes.

## Data deletion

Dedicated public instructions page:

`https://nuvanx.com/eliminacion-datos-meta/`

The Meta Dashboard was previously saved with the wrong User Data Deletion URL:

`https://nuvanx.com/politica-privacidad/`

### Required pre-submit action

In Meta App Dashboard → Basic Settings, update **User Data Deletion / Data deletion instructions URL** to:

`https://nuvanx.com/eliminacion-datos-meta/`

Then save and capture UI evidence. Graph v22 probes used in the production audit did not return the tested data-deletion/app-mode fields, so the Dashboard save state is the canonical evidence source for this setting.

## Business Verification

Submission `1836338617331298` showed NUVANX Medicina Estética Láser business verification as **En revisión** on 2026-08-24. App Review itself remained **No enviada**.

Do not describe Business Verification as complete until Meta changes this status.

## Reviewer-account gate

Production currently has no auth user whose email name indicates a dedicated Meta/review account. Before submission:

1. Create a dedicated reviewer account in the production Supabase Auth project.
2. Confirm email/password login against the stable Vercel production URL.
3. Ensure that account has access to the same NUVANX Meta integration/data needed for the review without exposing unrelated personal assets.
4. Verify Marketing → Meta Ads and CRM paths with that exact account.
5. Store the password only in the Meta submission/reviewer credential field, not GitHub.

## Final pre-submit checklist

- [ ] Business Verification is no longer `En revisión`.
- [ ] App Review permission set contains only the eight requested items in this dossier.
- [ ] Deferred permissions/features are removed from this submission.
- [ ] Data deletion Dashboard URL is saved as `https://nuvanx.com/eliminacion-datos-meta/`.
- [ ] Dedicated reviewer account exists and login has been tested in production.
- [ ] Reviewer account can open Marketing → Meta Ads and CRM.
- [ ] A bounded `ads_management` web action is deployed and tested with the reviewer account.
- [ ] The exact `ads_management` reviewer steps replace the provisional step 8 in `instructions-web-2`.
- [ ] Permission narratives match the deployed behavior; no hypothetical UI actions are described.
- [ ] Review screenshots/video show the stable production URL and the exact reviewer account workflow.
- [ ] Submission is sent only after all checks above are green.

## Architecture boundary

Keep these two planes separate in the review narrative:

- **Control plane:** `NUVANX Reporting` → Marketing API → Campaigns / Ad Sets / Ads / Creatives / Insights / Lead Ads.
- **Measurement plane:** WordPress → Supabase `web-events` → Meta Conversions API → Pixel/Dataset.

Do not justify Marketing API permissions with Conversions API functionality and do not request CAPI-related scopes merely because the same backend also contains measurement code.
