# Branch protection acceptance marker

This harmless documentation change exists to exercise the protected `main` path through pull request checks. It contains no runtime, credential, migration, or deployment changes.
