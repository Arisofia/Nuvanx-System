{
  "buildCommand": "cd frontend && rm -rf dist node_modules/.vite && npm run build && mv dist dist-new",
  "outputDirectory": "frontend/dist-new",
  "installCommand": "npm run install:all",
  "routes": [
    {
      "src": "/assets/(.*)",
      "headers": {
        "Cache-Control": "public, max-age=31536000, immutable"
      }
    },
    {
      "src": "/api/(.*)",
      "dest": "https://ssvvuuysgxyqvmovrlvk.supabase.co/functions/v1/api/$1"
    },
    {
      "handle": "filesystem"
    },
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
